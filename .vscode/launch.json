{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "playground",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/playground.py",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceRoot}"
            }
        }
    ]
}
